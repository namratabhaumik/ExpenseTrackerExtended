apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-frontend
spec:
  selector:
    app: expense-tracker-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
